global:
  k8s_node_name: cloud-gauntlet-master
gitea:
  image:
    repository: localhost:5000/gitea/gitea
    tag: 1.20.5
  ingress:
    enabled: true
    host: gitea.local
    className: traefik
  database:
    type: postgres
    host: gitea-db
    port: 5432
    user: gitea
    name: gitea
    existingSecret: gitea-db-secret

postgresql:
  enabled: true
  name: gitea-db
  storage:
    size: 512Mi
    storageClassName: local-path